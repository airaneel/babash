services:
  babash:
    image: airaneel/babash:latest
    ports:
      - "${BABASH_PORT:-8484}:8000"
    environment:
      - BABASH_SERVER_INSTRUCTIONS=${BABASH_SERVER_INSTRUCTIONS:-}
      - BABASH_SHELL=${BABASH_SHELL:-/bin/bash}
      - BABASH_DEBUG=${BABASH_DEBUG:-}
    entrypoint: ["babash_mcp", "--transport", "streamable-http"]
    volumes:
      - workspace:/workspace
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  workspace:
